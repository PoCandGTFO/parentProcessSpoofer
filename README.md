Change notepad.exe with your process

Process appear as if it was launched by a legitimate parent (in this case explorer.exe)

You can change the parent but aware not SYSTEM process like winlogon.exe
